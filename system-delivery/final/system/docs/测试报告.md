# 测试报告

## 测试环境

| 项目 | 实际值 |
|---|---|
| 操作系统 | Windows 11 22H2（x64） |
| Node.js | v24.19.0 |
| 浏览器 | Microsoft Edge（Chromium 内核，`--headless=new` 远程调试模式） |
| 服务器 | `server/server.js`（零依赖静态服务器） |
| 测试日期 | 2026-08 |

## 执行的测试命令与结果

### 1. 静态检查 — `node tools/project-check.mjs`

```text
✓ 必需文件完整（14 项）
✓ JavaScript 语法检查通过（36 个文件）
✓ HTML id 唯一（217 个）
✓ HTML 本地资源引用完整（7 项）
✓ 安全基线检查通过
结果：全部静态检查通过（退出码 0）
```

### 2. 单元 / 回归测试 — `node tools/regression-test.mjs`

**结果：31 通过，0 失败，0 跳过。** 覆盖：

| 组 | 内容 | 结果 |
|---|---|---|
| [1] | csvCell 转义（负数、公式注入防护、null/undefined） | 5 ✓ |
| [1.1] | 本地配置安全合并（类型校验、原型污染、恢复默认） | 4 ✓ |
| [2] | 模拟链路 SimDriver→Indicators→Fusion→Recorder（90s 剧本：最高分 68.0、等级 moderate、171 采样点、正常眨眼均 ≤500ms） | 4 ✓ |
| [3] | unreliable 无效数据门控（faceLostRatio>0.5） | 1 ✓ |
| [4] | CSV 往返：导出（BOM+中文表头）→解析→离线重算（171 样本一致、首行等级一致、pitch 负数格式正确） | 9 ✓ |
| [5] | 语义否决：姿态 EAR 假阳性场景下 microsleep/critical_closure 为 0 | 2 ✓ |
| [6] | 无人脸场景报告：insufficient=true 且不出现「状态良好」 | 3 ✓ |
| [7] | 评测指标计算（混淆矩阵 TP=1 FP=1、忽略区间） | 3 ✓ |

### 3. 集成 / 安全测试 — `node tools/integration-test.mjs --port 5181`

**结果：41 通过，0 失败。** 覆盖：

- [1] 7 个关键资源 200 + MIME 正确（含 `application/wasm`、`.task` 二进制）
- [2] 6 项安全响应头齐备（CSP / nosniff / Permissions-Policy / Referrer-Policy / COOP / frame-ancestors）
- [3] 10 项穿越与非法请求防护（原始 HTTP 请求发送 `/../`、`%2e%2e`、`%2f`、空字节、POST 405、404 不泄露磁盘路径）
- [4] Range 请求 206 + 长度精确 + wasm 魔数 `\0asm` 校验
- [5] 首页 7 个关键元素存在 + 无外部域名引用（离线可用）

### 4. 一键启动链路实测

- 命令：`node tools/launch.js --no-open --port 5185`
- 输出：`[1/3] Node.js 环境正常 (v24.19.0)` → `[2/3] 本地推理资源已就绪，可离线运行` →
  `[3/3] 正在启动本地服务` → 访问地址 `http://127.0.0.1:5185/`
- HTTP 探测：`GET /` 返回 **200**。
- **中文路径实测通过**（项目位于含中文与空格的桌面目录，此处不写本机绝对路径）。
- 端口冲突实测：5180 被占用时服务器自动切换 5181 并打印正确地址。
- 非法端口实测：`--port abc` 与 `--port 99999` 均中文报错并以退出码 1 终止。

### 5. 浏览器运行取证（无头 Edge + CDP）

对优化版与原始版快照各执行一轮完整取证（首页/滚动/深色、工作台深浅色、
专业模式、设置抽屉、报告页深浅色、1366×768、390×844 移动端，共 13 张/轮）：

- 两版页面标题、三视图导航、设置抽屉、报告页渲染全部正常；
- **控制台 error 级别报错：0 条**（CDP `Runtime.consoleAPICalled` / `exceptionThrown` 监听）；
- 截图保存于 `system-delivery/comparison/before/` 与 `comparison/after/`。

## 未验证项（如实列出）

| 项目 | 原因 |
|---|---|
| 真实摄像头授权与实时检测帧率 | 无头浏览器无法授权摄像头，需人工在物理设备上验证 |
| 模型实际加载耗时与 WebGL 推理帧率 | 同上（headless 无 WebGL 摄像头链路） |
| Firefox / Safari 兼容性 | 当前环境仅有 Chromium 内核浏览器 |
| 声音报警与语音播报实际效果 | 需有声设备人工验证（代码链路审查通过） |
| 长会话（>30min）内存曲线 | 需真人长时间会话采集 |
| Windows 10 / 双击 .bat 图形流程 | 本机为 Win11；.bat 内容与 launch.js 链路已实测，双击动作本身未演练 |
| A-07/08/09 文件拦截的浏览器端表现 | 需人工选择真实超大文件复核（代码与静态检查已通过） |

## 结论

自动化测试共 **72 条断言通过、0 失败**（静态检查 5 项 + 回归 31 + 集成 41，
其中静态检查按项计），一键启动链路与端口容错实测通过，
两轮浏览器取证控制台零报错。所有未验证项已在上表如实标注。
