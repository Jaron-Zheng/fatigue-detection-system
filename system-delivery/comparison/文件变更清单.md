# 文件变更清单

基线：`system-delivery/original/system/`（原始快照，保持未修改）
现行：项目根目录（与 `system-delivery/final/system/` 内容一致）

## 一、修改的文件（16 个）

| 文件 | 规模 | 修改内容 |
|---|---|---|
| server/server.js | +35/-2 | 新增全套安全响应头（CSP/COOP/CORP/Permissions-Policy/Referrer-Policy）；`resolvePort()` 支持 `--port` 与非法端口中文报错；`resolveSafe()` 拒绝反斜杠与非绝对路径 |
| web/js/config.js | +19/-5 | `deepMerge` 重写：数字字段类型校验、原型污染键忽略、仅覆盖已知字段；`loadUserConfig` 支持注入 storage（可测试性） |
| web/js/core/video-source.js | +17/-0 | 视频文件校验（空/超1GB/非video MIME）；release() 清理 video 元素 |
| web/js/ui/analysis-ui.js | +8/-0 | 复现 CSV 校验（空/超32MB/格式） |
| web/js/ui/evaluation-ui.js | +5/-0 | 标注 JSON 校验（空/超2MB/格式） |
| web/js/ui/report.js | +9/-3 | 报告摘要 pill 由 innerHTML 改为 DOM API 构建 |
| web/js/ui/settings.js | +23/-1 | 设置抽屉 Tab 焦点陷阱（无障碍） |
| web/js/util/dom.js | +1/-1 | `el()` 的 html 属性改走 textContent，关闭 innerHTML 通道 |
| web/index.html | +4/-4 | 首屏四张能力卡增加 `data-hover-card` 悬停交互标记 |
| web/css/motion.css | +33/-5 | 新增悬停微交互段（轻推+缩放+图标跟随），限定 hover 设备与减弱动效偏好 |
| web/css/tokens.css | +3/-0 | 新增 `--dur-hover`、`--card-hover-lift`、`--card-hover-scale` |
| web/css/layout.css | +5/-2 | feat 卡/图标补 transform 过渡；报告 pill 字重 500→400（Apple 字阶无 medium） |
| web/css/components.css | +3/-1 | `.card-hover` 过渡补 transform 维度 |
| tools/launch.js | +13/-0 | Node ≥18 版本检查与中文提示 |
| tools/regression-test.mjs | +14/-1 | 新增 [1.1] 本地配置安全合并测试组 |
| package.json | +5/-2 | 新增 check/test/verify 脚本（后又补 test:integration）；描述微调 |

## 二、新增的文件

| 文件 | 说明 |
|---|---|
| 一键启动.ps1 | PowerShell 启动入口（-Port / -NoBrowser，含版本检查） |
| tools/project-check.mjs | 静态检查：文件完整性、JS 语法、id 唯一、资源引用、安全基线 |
| tools/integration-test.mjs | 服务器集成/安全测试（41 条断言） |
| tools/screenshot.mjs | 无头浏览器 CDP 批量截图取证工具 |
| CHANGELOG.md | 逐项变更记录 |
| docs/代码审计报告.md | 12 项审计问题与修复状态 |
| docs/Bug修复清单.md | 本轮修复 7 项 + 历史修复追溯 8 项 |
| docs/测试报告.md | 测试环境、命令、结果、未验证项 |
| docs/UI设计说明.md | Apple 风格设计系统与页面结构 |
| docs/算法变更说明.md | 算法未改动的证明与参数可追溯性 |
| docs/启动故障排查.md | 13 类常见问题处理 |
| README.md（更新） | 新增测试章节、目录结构补全、文档索引 |

## 三、截图取证（comparison/）

- `before/`：原始快照 13 张（首页浅/深/滚动/底部、工作台浅/深/专业模式、
  设置抽屉、报告浅/深、1366×768、390×844）
- `after/`：优化版同口径 13 张 + 既有 dashboard-dark-pro.png
- 两轮取证控制台 error 均为 0 条

## 四、未修改的部分（刻意保留）

- `web/js/core/` 全部算法模块（fusion/indicators/features/calibration/
  quality/recorder/csv-schema/analysis/evaluation/evaluator/sim-driver/
  landmarks/face-engine）——逐行审读确认无需改动，见算法变更说明；
- `web/vendor/` 模型与 WASM 资源（26MB，已本地化）；
- `一键启动.bat`（纯 ASCII 引导，已满足全部检查项）。
