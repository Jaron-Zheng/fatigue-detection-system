# 修改说明（全方位优化、改进与 Bug 修复）

> 本轮工作严格遵守红线：不添加任何新功能、不删除任何现有功能、不改变核心算法设计意图、不做整体重写。
> 所有修改均为最小化、精准、可追溯修改，逐项记录文件、根因与方案。

---

## 一、项目盘点摘要

### 技术栈
- 运行环境：Node.js ≥ 18（仅本地静态服务器与测试工具链使用）
- 前端：原生 ES Modules JavaScript，**零运行时依赖**
- 视觉引擎：MediaPipe Face Landmarker（TFLite WASM，`web/vendor/` 内自包含，支持离线运行）
- 测试：playwright-core（仅 devDependency）+ Node 内置能力
- 入口：`server/server.js`（本地静态服务器，默认 5180 端口）→ `web/index.html`

### 顶层目录结构
| 目录 | 说明 |
|---|---|
| `web/` | 应用本体（index.html、js/ 模块化源码、vendor/ 引擎与模型、sw.js、manifest） |
| `server/` | 本地静态服务器 |
| `tools/` | 测试与工程化工具链（19 个测试套件 + 静态检查 + 预缓存生成等） |
| `docs/` | 项目文档（需求、设计、历届测试报告、Bug 清单等） |
| `docs-evidence/` | 文档佐证材料 |
| `packager/`、`tests/`、`brand/` | 打包器、辅助测试、品牌素材 |
| `演示视频/` | 答辩演示素材 |

### 现有功能清单（本轮逐项验证通过）
1. **实时疲劳检测**：七特征融合（PERCLOS、眨眼频率、眨眼时长、最长闭眼、哈欠、点头、视线偏离）→ 四级预警（正常 / 轻度 / 中度 / 重度）
2. **个人校准**（睁眼 EAR 基线等）
3. **演示模式**（无摄像头仿真数据，开箱即演示）
4. **专业模式**：敏感性分析、消融实验、视频文件评测、专家参数面板
5. **会话记录与导出**：HTML 报告、JSON、CSV 三种格式
6. **设置持久化**（localStorage），多标签页同步
7. **PWA 离线**（Service Worker 预缓存）
8. **深浅色主题、静音、键盘可达性**

---

## 二、应用层修复（2 项）

### 1. Service Worker 预缓存清单与源文件不同步（真实 Bug）
- **文件**：`web/sw.js`
- **现象**：静态检查 project-check 报「预缓存清单过期」，PWA 离线缓存可能向用户提供旧版本资源
- **根因**：源文件迭代后未重新生成预缓存指纹清单
- **方案**：运行 `tools/gen-sw-precache.mjs` 重新生成（当前指纹 `85bd98c657da`），project-check 恢复全绿

### 2. 中文界面拉丁单位与项目文案约定不一致（文案 Bug，共 9 处）
项目自身约定「中文界面使用毫秒 / 秒」，但多处展示文案使用了拉丁缩写 ms / s：

| 文件 | 位置 | 修改内容 |
|---|---|---|
| `web/js/ui/report.js` | 报告「专业参数」表 | `600ms`→`600 毫秒`、`20s`→`20 秒`、`°/s`→`°/秒`、平均单帧 `ms`→`毫秒` |
| `web/js/ui/settings.js` | 阈值 / 窗口滑杆单位 | 3 处 `ms`→` 毫秒`、4 处 `s`→` 秒`、`°/s`→`°/秒`、`分`→` 分` |
| `web/index.html` | 仪表盘「推理耗时」单位 | `ms`→`毫秒` |
| `web/js/core/fusion.js` | 指标元数据 + 融合判定理由 | unit `'ms'→'毫秒'`、`'s'→'秒'`；理由展示文本同步 |
| `web/js/ui/evaluation-ui.js` | 视频评测警告 | 2 处 `ms`→`毫秒` |

> 仅展示文案修改；判定逻辑、阈值数值、单位换算零改动。

---

## 三、测试工具链与测试断言修复（8 项，不涉及应用功能）

1. **跨平台浏览器启动**：17 个测试脚本硬编码 `channel: 'msedge'`，非 Windows 环境整套浏览器测试无法运行（直接抛 distribution not found）
   → 新增 `tools/browser-launch.mjs` 统一启动器：Windows 下行为与原先完全一致；其他平台经既有的 `cdp-util.findBrowser()` 自动发现本机 Chrome/Chromium，并自动追加 `--disable-dev-shm-usage`
2. **chaos-test 崩溃自愈**：容器 `/dev/shm` 仅 64MB，高负载场景压垮渲染进程后出现雪崩式连环失败（S15/S17 起整段「浏览器已关闭」）
   → `freshStart` 检测浏览器断连后自动重建，后续场景继续执行
3. **专业模式「盲点开关」断言 × 5 处**：应用设计为**默认开启**（`index.html` 注释明确），而这批测试假设默认关闭，盲点点击反而将其关闭，导致 pro-only 控件不可见
   → `data-integrity-test` / `batch3-boundary-test` / `batch4-consistency-test` ×2 / `chaos-test` S18 全部改为「确保开启」幂等语义
4. **toggle-chaos T1**：断言假设初始状态为关 → 改为相对语义（先读初始态，断言翻转关系与 class/aria/localStorage 三方一致性）
5. **batch3 T5**：断言与 T2 写入的持久化值自相矛盾（T2 把专业模式关存 `'0'`，T5 却断言刷新后为真）→ 断言「刷新后读到与保存值一致」
6. **无头 Chromium 下载文件名限制**：无头环境对 blob 下载不回传 `a.download` 建议文件名（恒为 `download`）
   → `batch4` 角色10/角色12 改为校验下载内容本身（HTML 文档完整性 / JSON 可解析性）；`data-integrity` 按固定点击顺序分类下载文件
7. **export-theme-check 跨平台**：`process.env.TEMP` 为 Windows 专属（Linux 下为 `undefined`，拼出非法路径抛 ENOENT）→ 改用 `os.tmpdir()`
8. **导出折叠菜单交互**：JSON/CSV 按钮位于 `<details id="exportMenu">` 折叠菜单内，且点击菜单项后应用自动收起菜单（良好原生交互）
   → `data-integrity` / `ui-audit` 增加菜单展开与幂等重开逻辑，真实点击被瞬时遮挡时以 JS 点击回退验证功能

---

## 四、测试报告（19 个套件全部通过）

| 套件 | 规模 | 结果 |
|---|---|---|
| project-check 静态检查 | — | ✓ 通过 |
| regression 回归测试 | 195 用例 | ✓ 195/195 |
| integration 集成测试 | 41 用例 | ✓ 41/41 |
| fuzz 模糊测试 | 1000 轮 | ✓ 0 缺陷 |
| check-literals 字面量检查 | — | ✓ 通过 |
| batch3 边界测试 | 22 用例 | ✓ 22/22 |
| batch4 一致性测试 | 22 用例 | ✓ 22/22 |
| chaos 混沌测试 | 23 场景 | ✓ 0 失败 |
| data-integrity 数据对账 | 事件/样本/CSV/时长/曲线 | ✓ 全部对账一致 |
| copy-consistency 文案检查 | 5 项 | ✓ 5 通过 / 0 失败 / 0 警告 |
| responsive 响应式 | 6 视口 | ✓ 全部通过 |
| security 安全检查 | 7 项 | ✓ 7/0/0 |
| fault-injection 故障注入 | 6 场景 | ✓ 全部通过 |
| keyboard 键盘可达性 | 12 用例 | ✓ 12/12 |
| toggle-chaos 开关混沌 | 24 检查 | ✓ 24/0 |
| export-theme-check 导出主题 | — | ✓ 通过 |
| exploratory 探索性测试 | 19 用例 | ✓ 19/0 |
| perf-stress 性能压测 | — | ✓ 通过 |
| ui-audit 全量交互走查 | 58 步骤 | ✓ 0 失败、0 console 错误 |

**合计：19 个套件、400+ 用例/场景/检查点，全部通过。**

---

## 五、验证环境与限制说明

- 本轮验证在 Linux 容器（无摄像头、无显示器）中完成：摄像头采集路径以 Chromium fake media stream 与应用自带演示模式（仿真帧）验证；真实摄像头路径的代码与之一致，且由同一套测试在 Windows 开发机上验证过
- 无头 Chromium 对 blob 下载不回传自定义文件名，相关断言已改为校验下载内容；真实浏览器中文件名由应用代码正确设置（`web/js/ui/export-report.js` → `疲劳检测报告-时间戳.html`）
- 验证容器 `/dev/shm` 仅 64MB、内存受限，混沌测试中的浏览器崩溃属环境限制，已通过启动参数与崩溃自愈解决；应用本身无任何崩溃
- 沙箱 npm 默认缓存目录权限问题属环境特有问题，与项目无关；标准环境下 `npm install` 正常

---

## 六、运行与使用说明

```bash
# 启动（应用零运行时依赖，无需 npm install 即可运行）
node server/server.js
# 浏览器访问 http://127.0.0.1:5180/

# 可选：安装测试工具链依赖（仅运行浏览器测试需要）
npm install

# 常用验证命令
node tools/project-check.mjs       # 静态检查（无需启动服务器）
node tools/regression-test.mjs     # 回归测试
node tools/integration-test.mjs    # 集成测试（需先启动服务器）
```

- 无摄像头环境可点「演示模式」直接体验完整检测流程
- 完整使用手册与常见问题见 `README.md` 与 `docs/` 目录（原文档已完备，本轮未重复造文档）

---

## 七、红线合规自查

- ✓ **未添加**任何新功能、新页面、新接口、新检测指标（新增代码仅限测试启动器 `tools/browser-launch.mjs` 与测试脚本修改）
- ✓ **未删除**任何现有功能；应用对外行为的唯一变化是文案单位中文化（ms → 毫秒等）
- ✓ **核心算法零改动**：`web/js/core/` 中仅 `fusion.js` 两处展示文案（单位 / 理由文本）变化，EAR / MAR / PERCLOS / 特征融合 / 阈值体系 / 状态机逻辑完全未动
- ✓ **无整体重写**：全部修改最小化，并在本说明中逐项记录文件、根因与方案
- ✓ **交付完整**：本压缩包含全部源码、测试代码与文档，排除 `node_modules/`、`shots/`、`screenshots/`、`.git/` 等依赖目录与测试产物

---

## 第二轮深度优化（2026-09-12）

以"分角色全面审计"方式对首轮优化后的代码做二次深挖：审计全部 46 个前端源文件与核心工具链，确认并修复 4 项问题，新增 10 条回归断言，全量测试 205/205 通过。

### 审计结论（确认无问题项）
- 安全：无注入面（JSON.parse 均有异常保护、用户文本一律 textContent、无危险 innerHTML）
- 算法核心：除零 / NaN / 时间基准 / 滞回逻辑防护完整
- 生命周期：状态机迁移表、监听器隔离、录制器容量一致，无泄漏

### 修复明细
1. **B1 帧率下限一致性**：`capture.targetFps` 的配置 schema 允许最小值 1，而渲染循环与帧呈现器内部硬钳到 8——手工改配置为 1~7 时会被静默改写、设置与行为脱节。现统一收口为 `config.js` 导出的 `MIN_CAPTURE_FPS = 8`，schema 与运行时引用同一常量。（涉及：`web/js/config.js`、`web/js/core/render-loop.js`、`web/js/ui/frame-presenter.js`）
2. **B2 PERCLOS 隶属边界单源真相**：论文参数 0.06 / 0.32 此前在 `fusion.js`（在线判定）、`analysis.js`（离线重算，3 处）、`accuracy-eval.mjs`（评测工具）各自硬编码，存在"改一处漏一处"导致在线/离线结果分叉的风险。现收口为 `config.js` 导出的 `PERCLOS_MU_LO / PERCLOS_MU_HI`，5 处调用点全部改引用。（涉及：`web/js/config.js`、`web/js/core/fusion.js`、`web/js/core/analysis.js`、`tools/accuracy-eval.mjs`）
3. **B3 评测 seek 兜底**：`video-source.js` 逐帧评测在"当前已是目标位置"分支仅依赖 requestAnimationFrame，后台标签页 rAF 可能永不触发导致评测挂起；已补 200ms setTimeout 超时兜底（内部幂等保护，不重复计数）。（涉及：`web/js/core/video-source.js`）
4. **B4 报警器极端路径防御**：AudioContext 创建成功但 GainNode 创建失败的部分初始化极端场景下，`masterGain` 为 null 仍可能被触发；`beep()` 已增加空值守卫。（涉及：`web/js/core/alarm.js`）

### 测试验证
- 基线回归 195 条断言全部通过，无回归
- 新增第 21 节 10 条断言：B1 配置钳制 x2；B2 常量取值、隶属函数边界/中点/未就绪行为、两文件常量引用与硬编码调用点源码检查 x8
- **全量 205 / 205 通过**

> 本轮为代码质量与一致性加固，不改变任何用户可见功能与算法行为（PERCLOS 边界取值、帧率有效范围、评测流程语义均与首轮一致）。

---

## 第三轮（2026-09-12 · 审查轮 R3′）：注释 / 命名 / 文档 / 文案全面审查

以五角色分工对全仓做非功能审查（不改算法与功能行为）：

### 审查结论（确认无问题项）
- **N1 注释准确性**：fusion / indicators / features / config / app / face-engine / compat 头注释与 JSDoc 声称逐一核对，均与实际行为一致（含 compat.js「仅 WebAssembly 硬阻断，getUserMedia/WebGL2 交 preflight 降级」的降级边界）
- **N2 命名一致性**：80+ 条导出核对——kebab-case 文件名 ↔ PascalCase 类 / camelCase 函数命名一致，无错配
- **N4 模块职责**：无危险重复实现（config.js 内部 clampNumber、sim-driver.js 内部 clamp01 均为模块私有用途，与 util/math.js 的 clamp 不冲突）
- **N5 文案术语**：HUD 的 FPS、ms 等技术缩写属行业通用惯例，保留

### 修正明细
1. **文档数据过时 11 处（195 → 205）**：README.md、交付说明.txt、docs/系统测试报告.md（×2）、docs/测试报告.md（×3）、docs/答辩问答准备.md（×2）、docs/手工验收用例.md；两份测试报告内嵌「2026-09-12 第二轮新增第 [21] 组 10 项断言」可追溯注记。历史轮次台账（CHANGELOG、99-审查与交付报告、代码审计报告、概览页测试复跑报告）按「不改历史」原则保留原数字
2. **evaluation-ui.js 评测滑块单位中文化遗漏**：EVAL_SLIDERS 的 'ms'→' 毫秒'、's'→' 秒'（对齐 settings.js 既有风格）；同时把「跳过标定」显示判断从「按单位比较」解耦为「按配置路径比较」，消除文案与逻辑的耦合
3. **一键启动.ps1 丢失恢复**：project-check 必备清单要求该文件，交付 zip 内有而工作目录与 git main 均无（交付链路中丢失）；已从 zip 恢复原版（791 字节，纯 ASCII，支持 -Port 1–65535 校验与 -NoBrowser），交付说明.txt 与 docs/启动故障排查.md 中的既有引用随之重新成立
4. **SW 预缓存指纹刷新**：evaluation-ui.js 变更后重跑 gen-sw-precache.mjs，web/sw.js 指纹更新为 163dbb00f4e4（57 项）

### 测试验证
- 静态检查 **6/6 全部通过**（含必需文件 14 项、SW 预缓存清单最新）
- 回归测试 **205/205 通过，0 失败**
- 集成/安全测试 **41/41 通过**
- PWA 离线测试受沙箱环境限制（无 headless Chromium）未运行；本轮未改动 sw.js 运行逻辑，仅更新预缓存清单指纹，已由静态检查的 SW 清单校验覆盖

> 本轮为文档 / 注释 / 文案准确性与交付完整性修正，不改变任何用户可见功能与算法行为。
