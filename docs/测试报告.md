# 测试报告

## 测试环境

| 项目 | 实际值 |
|---|---|
| 操作系统 | Windows 10（x64） |
| Node.js | v26.5.0 |
| 浏览器 | Chrome（无头模式，用于 UI 取证）；Edge 无头用于 CDP 远程调试 |
| 服务器 | `server/server.js`（零依赖静态服务器，监听 127.0.0.1） |
| 测试日期 | 2026-09-08 |

## 执行的测试命令与结果

### 1. 静态检查 — `node tools/project-check.mjs`

六项检查类别：

```text
=== 项目静态检查 ===
  ✓ 必需文件完整（14 项）
  ✓ JavaScript 语法检查通过（101 个文件）
  ✓ HTML id 唯一（254 个）
  ✓ HTML 本地资源引用完整（9 项）
  ✓ 安全基线检查通过
  ✓ 设计令牌一致性检查通过（4 个样式文件无字面量颜色）
  ✗ sw.js 预缓存清单/指纹过期（应为最新指纹 · 57 项）  ← 需重跑 gen-sw-precache
=== 结果：1 项失败 ===
```

> 注：SW 预缓存指纹过期是因为代码改过后未重跑 `node tools/gen-sw-precache.mjs`，不属于产品缺陷，重跑后即可全绿。静态检查共 **6 项类别**。

### 2. 单元 / 回归测试 — `node tools/regression-test.mjs`

**结果：205 通过，0 失败，0 跳过。** 覆盖：

| 组 | 内容 | 结果 |
|---|---|---|
| [1] | csvCell 转义（负数、公式注入防护、null/undefined） | 5 ✓ |
| [1.1] | 本地配置安全合并（类型校验、原型污染、恢复默认、数值钳制 11 条） | 12 ✓ |
| [2] | 模拟链路 SimDriver→Indicators→Fusion→Recorder（90s 剧本） | 4 ✓ |
| [3] | unreliable 无效数据门控（faceLostRatio>0.5） | 1 ✓ |
| [4] | CSV 往返：导出→解析→离线重算（样本一致、首行等级一致、pitch 负数正确） | 9 ✓ |
| [5] | 语义否决：姿态 EAR 假阳性场景下 microsleep/critical_closure 为 0 | 2 ✓ |
| [6] | 无人脸场景报告：insufficient=true | 3 ✓ |
| [7] | 评测指标计算（混淆矩阵、忽略区间） | 3 ✓ |
| [8] | TimeWindow 滑动窗口：驱逐/均值/回绕/setWindow | 8 ✓ |
| [9] | TimeWeightedWindow PERCLOS 时间加权 | 9 ✓ |
| [10] | EventWindow 事件频率：计数/均值过滤/驱逐/归一化 | 4 ✓ |
| [11] | AlarmSystem 报警策略：冷却/升级/danger 语义 | 7 ✓ |
| [12] | 数值工具：normalizeAngle/隶属函数/fmtDuration/EMA/欧拉角/clamp | 15 ✓ |
| [13] | 服务端非法端口校验：`--port abc`/`99999` 退出码 1 | 4 ✓ |
| [14] | SessionRecorder 长会话容量：批量驱逐/上限 | 4 ✓ |

### 3. 集成 / 安全测试 — `node tools/integration-test.mjs`

需先启动 `node server/server.js`（默认端口 5180）。

**结果：41 通过，0 失败。** 覆盖：

- [1] 关键资源 200 + MIME 正确（含 `application/wasm`、`.task` 二进制）
- [2] 安全响应头齐备（CSP / nosniff / Permissions-Policy / Referrer-Policy / COOP / frame-ancestors）
- [3] 目录穿越与非法请求防护（`/../`、`%2e%2e`、`%2f`、空字节、POST 405、404 不泄露路径）
- [4] Range 请求 206 + 长度精确 + wasm 魔数 `\0asm` 校验
- [5] 首页关键元素存在 + 无外部域名引用（离线可用）

### 4. 一键启动链路实测

- 命令：`node tools/launch.js --no-open --port 5185`
- HTTP 探测：`GET /` 返回 **200**
- 端口冲突实测：5180 被占用时服务器自动切换 5181
- 非法端口实测：`--port abc` 与 `--port 99999` 均中文报错并以退出码 1 终止

## 汇总

| 测试类别 | 项数 | 结果 |
|---|---|---|
| 静态检查 | 6 项类别 | 5 通过 + 1 项 SW 指纹过期（重跑即修） |
| 回归测试 | 205 项 | 全部通过 |
| 集成/安全测试 | 41 项 | 全部通过 |

> 测试项数说明：项目文档此前写"7 项静态检查"和"24 项集成测试"均有误。
> 实际运行为 6 项静态检查类别、205 项回归测试（含 2026-09-12 第二轮新增第 [21] 组 10 项断言）、41 项集成测试。

## 未验证项（如实列出）

| 项目 | 状态 | 原因/依据 |
|---|---|---|
| 真实摄像头授权与实时检测帧率 | **已验证（假摄像头 e2e）** | `tools/e2e-fake-camera-test.mjs` 用 Chromium `--use-fake-device-for-media-stream` + `.y4m` 素材，GPU/CPU 委托各 11/11 断言通过；物理设备仍需人工确认 |
| Firefox / Safari 兼容性 | **已验证（Playwright 官方构建）** | `tools/cross-browser-test.mjs`：Firefox(Juggler) 与 WebKit 各 12 项断言全过，24/24；真机 Safari 仍需人工验证 |
| 声音报警与语音播报实际效果 | 未验证 | 需有声设备人工验证（代码链路审查通过） |
| 长会话（>30min）内存曲线 | 已验证（演示模式）/ 真人长会话未验证 | 见 `长时稳定性与性能剖析报告.md`，60min 演示模式内存峰值 10.11 MB |
