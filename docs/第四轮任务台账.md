# 第四轮任务台账

> 本台账按《第四轮优化提示词.md》角色编排建立，随执行持续更新。
> 状态：未开始 / 进行中 / 已完成 / 不适用 / 伪象（复核后判定为非缺陷）。
> 所有"已完成"附真实命令输出片段佐证。

## 阶段 0 · 基线（已完成）

- 环境修复：MinGit 2.47.1 重新部署至 `%USERPROFILE%\MinGit`（全路径调用）；node v24.19.0
- 垃圾清理：根目录 `.zip`（47.2MB 损坏下载残留）、`system-delivery/**/.backup/`、`tools/迅雷.lnk` 移除
- 基线验证：`node tools/full-verify.mjs` 全绿（静态 6 类 / 回归 125/0 / 集成 41/0 / typecheck / lint 0err）
- 基线固化：commit 33d3fd9，tag `v4.0.0-baseline`（固化第三轮后未入库的 Tesla 重设计 + CHANGELOG 3.1.0 真人反馈修复）

## 批次一 · 体验层（已完成）

### 审视发现（四角色并行）

| 角色 | 关键发现 |
|---|---|
| 美学总监 | 字号 11px 破档、灰阶不成镜像、占比三种口径、报警横幅分级弱、视频叠字对比不可控等 17 项 |
| 新手司机（45 岁） | 校准三套说法、无动作指令、声音开关无状态、摄像头下拉空、保存歧义等 21 项 |
| 产品经理 | ?demo 缺失、演示 102 秒空等、摄像头失败逃生口、报告空态、UI设计说明漂移等 16 项裁决 |
| 信息设计师 | 结论口径矛盾、占比色错配、单位 秒/s/ms 混用、小数位混乱、方向语义缺失、色盲编码不足等 25 项 |

### 伪象甄别（重要：三组"严重问题"经代码级复核为并行浏览器代理共用同一内置浏览器互相干扰的串扰，或重渲染中途快照伪象，非应用缺陷）

| 原始报告 | 复核结论 | 证据 |
|---|---|---|
| "导航跳到 /css/components.css、/js/app.js 源码页" | 伪象。页面无任何指向资源文件的可点击元素；`/js/charts.js` 在项目中不存在（实为 chart.js）必 404，人类用户点击不可能产生该 URL | index.html 全量 href 扫描（46 处无资源目标）+ view-router/export-report/motion 无导航代码 |
| "报警自动切换视图、'知道了'点完跳首页、页面自己动" | 伪象。alarm-visuals.js 无视图切换代码；btnDismissAlarm 仅 hideBanner()；两个 browser_use 代理并行操作同一 URL 互相干扰 | alarm-visuals.js grep（showBanner/hideBanner 无 switchView） |
| "行为表'长时间闭眼'行丢单元格 / 统计数字出现 -33、-0" | 伪象。EVENT_ROWS 渲染固定 3 单元格（el('tr') 一次构建）；countUp 缓动单调非负（仍已加钳位与重入保护防御） | report.js L151-163 结构固定；motion.js ease(t)=1-(1-t)³ 单调 |

**流程改进**：后续浏览器走查类角色一律串行执行，不再并行共用浏览器。

### 真缺陷修复（本人执行）

| # | 缺陷 | 修复 | 文件 |
|---|---|---|---|
| 1 | 首页收尾段「进入实时检测」死链（a[data-goto] 不在 .gn-links 内未被绑定，点击只跳 #） | 事件委托到 document，绑定全部 a[data-goto] | view-router.js |
| 2 | start()/stopSession 走 switchView 不更新导航 aria-current（从首页开始检测后导航高亮滞留） | 高亮逻辑下沉 switchView（单一事实来源） | view-router.js |
| 3 | 演示模式开启后设置抽屉遮挡工作台画面 | setSimulate 开启分支自动 hide() 并 gotoView('viewWork') | session-actions.js |
| 4 | 演示完整剧本 147 秒、重度在第 102 秒，答辩等不起；倍速会稀释 PERCLOS 窗口 | sim-driver 增加 START_OFFSETS 起点偏移（仅偏移 t0，阶段参数零改动） | sim-driver.js |
| 5 | 演示入口藏设置抽屉最底部 | `?demo=1/awake/mild/moderate` URL 直达（经 START→BOOTING→BEGIN_RUNNING 正规链路） | app.js + index.html 提示文案 |
| 6 | fusion.js 预热文案与 8 秒校准混淆（"积累闭眼数据 x/10 秒"） | 改为"闭眼占比统计预热中"明确区分两个概念（纯文案） | fusion.js |
| 7 | countUp 理论上可被中间态重触发 | 负值钳位 + rAF 重入保护 | motion.js |
| 8 | 校准完成无瞬间反馈 | showCalibrated() 800ms 完成提示 | session-stage.js（代理 C）+ app.js 挂点一行 |

### 展示层批量执行（四个并行代理，文件所有权隔离）

| 代理 | 范围 | 要点 |
|---|---|---|
| A 排版令牌 | tokens/base/layout/motion.css | 11px 清零、字号阶梯 40/32/26/20/16/14/12、4 档文字令牌浅深镜像（#8E8E8E 脏色清零）、tile 双行标签、分段 48px 节奏、pill 13px/1.2、severe 横幅整幅反色 6.0:1、HUD/胶囊墨底+模糊、figcaption 13px、gn-icon 36px 热区、行高归 1.5 |
| B 报告页 | report.js/chart.js/index.html(viewReport) | 空态引导卡+下载禁用、结论口径说明+危险闭眼高风险标注、占比一位小数、行为表去 └ 改"危险闭眼"、"疲劳提醒"移出+底部小字、频次"X.X 次/分"、峰值/权重/EAR 小数位统一、sim 假值占位、UA→"Chrome 142 · Windows"、时间戳补零、覆盖率→画面可用率、dist 重复块删除、MutationObserver 主题跟随、阈值图例、报告图悬停十字准线+数值气泡 |
| C 设置/舞台 | settings/session-stage/app-chrome/video-controls | 摄像头空态分场景文案、试听分级三按钮（含 settings-wiring 签名一处）、静音状态标识（icon+aria-pressed+title）、校准指令含 CONFIG 时长、GPU 串去十六进制 ID 截断、origin 只留 host |
| D 数据展示 | dashboard/timeline/frame-presenter/workbench-charts/overlay | 均值一位小数、眨眼卡方向语义（低/高=危险）、spark 状态色+title、事件标签映射"长闭眼 ≥0.5s"、事件分级 data-level、sim FPS 占位"--"、工作台图参考线确认已有、stage 等级描边类（level-warn/danger） |

### 接口缝合（本人执行）

- app.js `_finishCalibration` 挂 `showCalibrated()`（代理 C 报告的必要一行）
- 试听说明文案、hero 次按钮图标 16→18（代理 A/B 报告的 HTML 侧改动）
- `.stage.level-warn/.level-danger` outline 描边（box-shadow 被 design-audit R2 零投影规则拦截，改 outline 等效实现）
- 指标卡重构：「当前闭眼时长」降 pro-only，「眨眼频率」升简洁模式（6 卡平衡，普通人最易懂"眨眼变慢=困"）

### 批次一验证输出（真实片段）

```text
node tools/demo-url-test.mjs（原 r4-smoke） → 12 通过, 0 失败
  ✓ ?demo=moderate：工作台激活/导航高亮/演示中/抽屉关/遮罩隐/剧本处于中度
  ✓ 首页「进入实时检测」点击进入工作台（死链已修）
  ✓ 报告空态：引导卡可见/下载禁用/动作按钮在
node tools/full-verify.mjs → 全部通过（静态/回归125/集成41/typecheck/lint 0err）
node tools/design-audit.mjs → 14 通过, 0 失败
node tools/a11y-test.mjs → 14 通过, 0 失败（critical/serious=0）
node tools/ui-smoke.mjs → 14 通过, 0 失败
node tools/screenshot.mjs → 13 张截图，控制台错误：无
```

### 批次一遗留（转入后续批次）

| 项 | 去向 |
|---|---|
| UI设计说明.md 仍是 Apple 体系（产品经理 P0 #4） | 批次三·文档工程师 |
| 历史会话列表（产品经理 P2 #11） | 循环二评估 |
| 专业模式报告页锚点（P2 #12） | 循环二评估 |
| A11 自适应（说明见 UI设计说明） | 随批次三文档更新 |

---

## 批次二 · 工程层（已完成）

### 审视发现（四角色并行）

| 角色 | 结论摘要 |
|---|---|
| 前端架构师 | **无 P0**。P1×7：死 CSS（.dist-stat-* 等）、**report.js LineChart 监听器累积（唯一累积性缺陷）**、pad2 重复 5 处、僵尸令牌 25 个、.visible 死规则、refreshColors 漏刷 refLines、内联样式违纪律 |
| 性能工程师 | **无 P0**。与第三轮基线实测对比：infer p95 91→84ms（改善 8%）、内存无泄漏（15min 首末 2.81→2.97MB）。P1×1：报告图悬停无节流（125Hz 鼠标=每秒 125 次全量重绘）。P2：explain 每帧计算、网格 156 次独立 fill、shadowBlur 常态化。26MB vendor 全部按需懒加载 ✓ |
| 安全研究员 | **P0=0**（10 组新探测向量 + 3 组注入验证 + 15 秒全网络监听通过）。P1×1：vendor bundle 含 Google 休眠遥测（死代码+CSP 双重兜底，实测零外发，建议文档口径修正）。P2 若干：全部可接受并记录加固建议 |
| 测试工程师 | **P0×4**：screenshot.mjs 会截错误页当证据（实验复现）、控制台错误不影响退出码、demo-url-test 零错误收集、setSimulate 关抽屉零覆盖。P1：恒真断言×2、第四轮 UI 改动多数零覆盖、端口冲突 |

### 执行落实

| # | 修复 | 文件 |
|---|---|---|
| E1 | LineChart 实例复用（监听器只挂一次）+ 报告图主题色重取补强 | report.js / chart.js |
| E2 | 悬停 rAF 合帧（每帧至多一次重绘） | chart.js |
| E3 | refreshColors 补刷 3 条参考线取色 | workbench-charts.js |
| E4 | 网格点 156 次 fill→1 次 Path2D；睁眼态 shadowBlur 5→0 | overlay.js |
| E5 | explain() 计算并入 66ms 门槛（首帧立即出） | frame-presenter.js |
| E6 | 死 CSS 清理约 100 行（.dist-stat-*/.visible/.t-center/.card-hover/--ios-blue/--ls-*/--sp-17 等，逐项 grep 零引用后删） | 5 个 CSS |
| E7 | 内联样式进 CSS（.report-empty 系列/.overlay-actions/.sheet-foot-note/.span-full + 脆弱选择器改造） | CSS + index.html（本人配合 7 处替换） |
| E8 | server.js：.d.ts 死配置删除 + COEP: require-corp 纵深头（集成 41/0 无回归） | server.js |
| E9 | deepMerge 数组形状校验（堵 localStorage 整组替换 fusion.levels 注入面） | config.js |
| E10 | ReportView hooks 注入解耦空态按钮（app.js 本人配合一行） | report.js / app.js |
| F1 | screenshot.mjs：页面加载断言（title+readyState 轮询）+ 控制台错误 exit 1 + evalJs 全部包 IIFE | screenshot.mjs |
| F2 | demo-url-test 重构：cdp-util 化 + 无控制台错误断言 + 恒真修复 + 新探针 4（IDLE 拨开关关抽屉不跳转）/4B（会话中拨开关自动跳工作台）/5（?demo=awake 锁映射）+ 端口 9353 | demo-url-test.mjs |
| F3 | ui-smoke 补 2 断言（报告导航高亮 / 均值一位小数） | ui-smoke.mjs |
| F4 | 回归恒真断言改真断言（负 pitch CSV 往返 5 条） | regression-test.mjs |
| F5/F6 | perf-profile 端口 9349 / cdp-util rmSync 防翻转 | perf-profile / cdp-util |

### 批次二验证输出（真实片段）

```text
node tools/full-verify.mjs        → 全部通过（回归 129/0，较基线 +4 条新真断言）
node tools/demo-url-test.mjs      → 26 通过, 0 失败（探针 4/4B/5 全过）
node tools/a11y-test.mjs          → 14 通过, 0 失败
node tools/design-audit.mjs       → 14 通过, 0 失败
node tools/ui-smoke.mjs           → 16 通过, 0 失败
node tools/screenshot.mjs         → 13 张 + 控制台错误：无
失败路径实测 --url 无服务端口      → 退出码 1，错误信息含"页面未正确加载，标题=127.0.0.1"
```

### 安全口径修正（批次三文档落实）

- 隐私承诺措辞改为「应用自有代码零外发；第三方 bundle 的休眠遥测被 CSP 阻断（实测零外发请求）」
- fetch-vendor.js 升级 vendor 时复查 odml.pa.googleapis.com + 补 sha256 完整性校验建议
